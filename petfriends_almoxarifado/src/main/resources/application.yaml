spring:
  application:
    name: petfriends_almoxarifado
  datasource:
    url: jdbc:h2:mem:almoxarifado
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
